import React, { Component } from 'react';
import { removeAccessToken, removeUser, getAccessToken, getUser } from '../../config/LocalStorage';

class HealthStatus extends Component {

    render() {
        return (
            <div>
                ################HealthStatus################
            </div>
        );
    }
}

export default HealthStatus;